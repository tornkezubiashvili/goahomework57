function processArray(arr) {
  for (let i = 0; i < arr.length; i++) {
    console.log("ელემენტი:", arr[i], "| ტიპი:", typeof arr[i]);
  }
}


processArray(["გიორგი", 14, "საბა", 90.5, "ირაკლი", true]);